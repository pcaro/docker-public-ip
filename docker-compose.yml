version: '3.8'

services:
  public-ip-monitor:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - CHECK_INTERVAL=30
      - WEB_PORT=8080
      - WEB_HOST=0.0.0.0
      - DB_PATH=/data/ip_history.db
    restart: unless-stopped